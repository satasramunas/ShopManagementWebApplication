@using ShopManagementWebApp.Dtos
@model List<UserDto>

<h3>Testing Shop</h3>


<table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">FullName</th>
      
    </tr>
  </thead>
  <tbody>
      @foreach (var item in @Model)
        {
        <tr>
          <th scope="row"></th>
          <td>@item.FullName</td>
        </tr>
        }
    
    
  </tbody>
</table>